# ──────────────────────────────────────────────────────────────────────────────
# LocalCowork — Environment Configuration
# Copy to `.env` and adjust for your local setup. All values below are defaults.
# ──────────────────────────────────────────────────────────────────────────────

# ─── Model Configuration ─────────────────────────────────────────────────────

# Directory containing GGUF model files (downloaded from HuggingFace)
# LOCALCOWORK_MODELS_DIR=~/Projects/_models

# HuggingFace cache directory (defaults to ~/.cache/huggingface)
# Set to LOCALCOWORK_MODELS_DIR to keep all model files together
# HF_HOME=

# Text model API endpoint (OpenAI-compatible). Set by start-model.sh.
# Default when using LFM2 via llama-server: http://localhost:8080/v1
# Default when using Ollama:                http://localhost:11434/v1
# LOCALCOWORK_MODEL_ENDPOINT=http://localhost:8080/v1

# Vision model endpoint (for AI-powered OCR — optional, falls back to Tesseract)
# LOCALCOWORK_VISION_ENDPOINT=http://localhost:8081/v1
# LOCALCOWORK_VISION_MODEL=LFM2.5-VL-1.6B

# ─── Data & Storage ─────────────────────────────────────────────────────────

# App data directory (databases, logs, permissions).
# Defaults: macOS ~/Library/Application Support/com.localcowork.app/
#           Linux $XDG_DATA_HOME/com.localcowork.app/
# LOCALCOWORK_DATA_DIR=

# Per-server database overrides (default: inside LOCALCOWORK_DATA_DIR)
# LOCALCOWORK_TASK_DB=
# LOCALCOWORK_CALENDAR_DB=
# LOCALCOWORK_EMAIL_DB=
# LOCALCOWORK_AUDIT_DB=

# ─── Security ───────────────────────────────────────────────────────────────

# Colon-delimited sandbox paths for MCP file operations (set by agent at runtime)
# LOCALCOWORK_ALLOWED_PATHS=~/Projects:~/Documents

# ─── Development & Debug ────────────────────────────────────────────────────

# Rust tracing level
# RUST_LOG=localcowork=info,warn

# Override project root detection (rare — for non-standard layouts)
# LOCALCOWORK_PROJECT_ROOT=

# Whisper model path for meeting transcription (optional)
# WHISPER_MODEL_PATH=
